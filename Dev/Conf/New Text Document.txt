Ubuntu 20.04
root pass:
Root@HVPS2024
================================


python3 --version
pip3 --version
sudo apt-get install -y python3-pip | or sudo apt install python3-pip
/sudo reboot
sudo apt-get update
sudo apt-get upgrade
/sudo reboot

sudo apt-get install libpq-dev libjpeg-dev
sudo apt-get install curl ca-certificates gnupg
curl https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
sudo sh -c 'echo "deb https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
sudo apt install python3-pip
sudo apt update && sudo apt-get update && sudo apt upgrade && sudo apt-get upgrade
sudo apt install postgresql postgresql-client | or sudo apt-get install postgresql
sudo su 
sudo -u postgres createuser -d -R -S $USER 	---------->>> Create postgres user: odoo
createdb $USER 	---------->>> Create postgres Database: odoo
psql -w odoo	------------------>> run postgres with odoo postgres  user
then --> alter user postgres with password NULL ------------>> no need if the same ubuntu username "odoo"
sudo service postgresql restart
/sudo reboot
psql -l 	--------->> showing the databases
sudo apt-get install git-all
/sudo reboot

sudo -u postgres dropdb
or
sudo systemctl disable postgresql
sudo apt-get purge postgresql-
sudo apt-get autoremove
sudo rm -rf /var/lib/postgresql
sudo rm -rf /etc/postgresql
sudo find / -iname "*postgres*" -exec rm -rf {} \;
dpkg -l | grep postgresql
sudo apt remove --purge postgresql
sudo ls -l /var/lib/dpkg/info | grep -i postgresql
sudo mv /var/lib/dpkg/info/postgresql.* /tmp
sudo apt update
sudo apt autoremove
sudo apt clean

sudo dpkg -i --force-overwrite /var/cache/apt/archives/postgresql



sudo apt-get remove --purge postgresql | or sudo dpkg --remove postgresql-
sudo reboot
sudo -u postgres createuser -d -R -S odoo
sudo service postgresql restart
alter user odoo with password '123456'
createdb odoo

